import React from "react";
import classes from "./MainItem.module.css";

function MainItem({ id, ulId, image, title }) {
  return (
    <></>
      // <img className={classes.img} src={image} alt={title} />
  );
}

export default MainItem;
